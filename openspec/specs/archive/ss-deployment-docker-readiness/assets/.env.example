# Stata host-mounted provisioning (example).
#
# Usage:
#   cd openspec/specs/ss-deployment-docker-readiness/assets
#   cp .env.example .env
#   docker compose -f docker-compose.stata-host-mount.yml up

SS_ENV=production
SS_LOG_LEVEL=INFO

# Worker requires an explicit provider at startup; `offline` is sufficient for worker-only validation.
SS_LLM_PROVIDER=offline

# Host path to your Linux Stata install dir (example).
SS_STATA_HOST_DIR=/opt/stata18

# Mounted path inside the container (fixed by docker-compose.stata-host-mount.yml).
SS_STATA_CMD=/mnt/stata/stata-mp

# Do-template library location inside the container (repo is mounted read-only at /app).
SS_DO_TEMPLATE_LIBRARY_DIR=/app/assets/stata_do_library
