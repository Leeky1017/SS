# Releases

This folder is for build artifacts meant to be copied to a remote server (non-docker).

- Build: `scripts/build_release_nodocker.sh`
- Output:
  - Linux/macOS: `releases/ss-<version>-nodocker-<timestamp>.tar.gz`
  - Windows: `releases/ss-<version>-nodocker-<timestamp>.zip`
