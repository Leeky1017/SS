# Notes: issue-503-real-stata-e2e-gate

## Context
- Target runtime: Windows Server `47.98.174.3`, runtime dir `C:\\SS_runtime`, schtasks `SS API` / `SS WORKER`.
- Goal: replace post-deploy health-only gating with a real v1 E2E gate (no dual-path).

## Decisions
- (append as discovered)

## Findings / Hypotheses
- (append as discovered)

## Later
- (append non-blocking ideas here)

