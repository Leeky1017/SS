# Tasks — ISSUE-280 (Phase 4.10 Finance TK01–TK20)

- [ ] Add smoke-suite manifest for `TK01`–`TK20` (fixtures + params).
- [ ] Run Stata 18 smoke suite; triage failures with evidence report.
- [ ] Fix runtime errors to reach 0 `fail` within scope.
- [ ] Normalize anchors to `SS_EVENT|k=v` and standardize dependency + output types (`graph`, not `figure`).
- [ ] Run `ruff check .`, `pytest -q`, and `openspec validate --specs --strict --no-interactive`.
- [ ] Open PR (`Closes #280`), enable auto-merge, verify merged, sync main, cleanup worktree.

