## 1. Implementation
- [ ] 1.1 Define composition plan schema + validator (dataset_ref, products, mode consistency)
- [ ] 1.2 Add planner routing for `composition_mode` and minimal sequential plans

## 2. Testing
- [ ] 2.1 Add fixtures for merge-then-sequential and parallel-then-aggregate plans
- [ ] 2.2 Add unit tests for validation failures + routing behavior

## 3. Documentation
- [ ] 3.1 Update task card metadata + run log
