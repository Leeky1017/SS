# Archived: Docker deployment assets

SS is deployed without Docker.

This folder preserves historical Docker deployment files (for reference only):
- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`

These files are not part of the supported deployment path.
